checkTemp (temperatuur) 
{if (temperatuur >=0 && temperature <=20)} 
{return -1; // liiga kylm
else if (temperatuur >= 21 &&
    temperatuur <=40)}
{return 0; // ideaalne temperatuur}
else if (temperatuur >=41 && temperatuur <=60)
{return 1; // liiga kuum
